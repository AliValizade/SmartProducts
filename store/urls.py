from django.urls import path
from . import views

# app_name = 'store'

urlpatterns = [
    # path('home/', views.HomePageView.as_view(), name='home'),
    # path('aboutus/', views.AboutUsView.as_view(), name='about_us'),
    # path('', views.ProductListView.as_view(), name='product_list'),
    # path('<int:pk>/', views.ProductDetailView.as_view(), name='product_detail'),
    # path('comment/<int:product_id>/', views.CommentCreateView.as_view(), name='comment_create'),
    # path('women/', views.WomenProductListView.as_view(), name='women_shop'),
    # path('men/', views.MenProductListView.as_view(), name='men_shop'),
    # path('new/', views.NewProductsListView.as_view(), name='new_product'),
    # path('popular/', views.popular_products, name='popular_products'),
    # path('bestsell/', views.BestSellProductsView.as_view(), name='best_sell_products'),
    # path('checkout/', views.order_checkout, name='checkout'),
    # path("profile/", views.MyAccountView.as_view(), name="my_account"),
    # path("payment/", views.PaymentView.as_view(), name="payment_process"),     
    # path('wishlist/', views.WishlistView.as_view(), name='wishlist'),
    # path('add-to-wishlist/<int:product_id>/', views.add_to_wishlist, name='add_to_wishlist'),
    # path('blog/', views.posts_list_view, name='blog'),
]
